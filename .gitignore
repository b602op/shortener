# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Binaries
cmd/shortener/main
cmd/shortener/shortener

# Dependency directories (remove the comment below to include it)
vendor/

# IDEs directories
.idea
.vscode

# Данные хранилища
data/storage.json
data/*.json

# Multichecker binary (только в корне репозитория)
/staticlint
/staticlint.exe

# Reset generator binary (только в корне репозитория)
/reset
/reset.exe

# Бинарники сборки
bin/
/shortener
/shortener.exe

# Test artifacts
test_storage*.json
test_storage_insert.json

# TLS-сертификаты
certs/*.pem
*.pem
